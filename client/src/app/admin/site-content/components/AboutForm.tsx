import React from 'react';
import { AboutContent } from '@/hooks/useSiteContent';

// Placeholder for other forms to ensure build passes
export default function AboutForm({ content, onSave, saving }: any) {
    return <div className="p-4 text-center">About Form Implementation</div>;
}
