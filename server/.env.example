# ============================================
# SK Production - Server Environment Variables
# ============================================
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun:
# cp .env.example .env

# ============================================
# Genel Ayarlar
# ============================================
NODE_ENV=development
PORT=5001

# ============================================
# MongoDB Database
# ============================================
# MongoDB Atlas veya local MongoDB bağlantı string'i
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/skproduction
# Alternatif: MONGODB_URI (geriye dönük uyumluluk için)
# MONGODB_URI=mongodb://localhost:27017/skproduction

# ============================================
# JWT Authentication
# ============================================
# JWT Secret Key (güçlü bir random string kullanın)
# Örnek: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production

# ============================================
# CORS & Client URL
# ============================================
# Frontend URL'i (CORS için)
CLIENT_URL=http://localhost:3000
# Alternatif CORS origin (virgülle ayrılmış)
# CORS_ORIGIN=http://localhost:3000,https://skproduction.com

# ============================================
# Redis Cache (Opsiyonel)
# ============================================
# Redis bağlantı URL'i (cache için)
# Redis yoksa uygulama çalışmaya devam eder, sadece cache devre dışı kalır
REDIS_URL=redis://localhost:6379

# ============================================
# Email Service (SMTP) - Contact Form
# ============================================
# İletişim formu için email gönderimi (Opsiyonel)
# Gmail veya Google Workspace için:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_SECURE=false
SMTP_FROM_NAME="SK Production"
SMTP_FROM_EMAIL=your-email@gmail.com
CONTACT_FORM_TO_EMAIL=info@yourdomain.com

# Google App Password oluşturma:
# 1. Google Account > Security > 2-Step Verification (aktif olmalı)
# 2. App Passwords > Select app: Mail > Select device: Other
# 3. Oluşturulan 16 haneli şifreyi SMTP_PASS olarak kullanın
# 
# NOT: SMTP ayarları yapılmazsa formlar sadece veritabanına kaydedilir,
# email gönderilmez. Uygulama normal çalışmaya devam eder.

# ============================================
# Push Notifications (VAPID Keys)
# ============================================
# Web Push API için VAPID keys
# Oluşturma: npm install -g web-push && web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_SUBJECT=mailto:info@skproduction.com

# ============================================
# ngrok (Development - Opsiyonel)
# ============================================
# ngrok auth token (development için)
# Dashboard: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTH_TOKEN=your-ngrok-auth-token
# Opsiyonel: Özel subdomain (Pro plan gerekli)
# NGROK_SUBDOMAIN=your-subdomain

# ============================================
# Logging
# ============================================
# Log seviyesi: error, warn, info, debug
LOG_LEVEL=info

# ============================================
# Production Notları
# ============================================
# Production'da:
# 1. NODE_ENV=production olarak ayarlayın
# 2. JWT_SECRET ve JWT_REFRESH_SECRET'ı güçlü random string'lerle değiştirin
# 3. MONGO_URI'yi production MongoDB Atlas connection string'i ile değiştirin
# 4. CLIENT_URL'yi production frontend URL'i ile değiştirin
# 5. SMTP ayarlarını production email servisi ile değiştirin
# 6. VAPID keys'i production için oluşturun
# 7. REDIS_URL'yi production Redis instance'ı ile değiştirin (varsa)
